# 每次啟動步驟

---

## Step 1：開啟 Docker Desktop

從開始選單開啟 **Docker Desktop**，等到右下角通知圖示顯示「Docker Desktop is running」。

---

## Step 2：開啟 PowerShell，執行部署腳本（推薦）

```powershell
cd C:\Users\jason\Desktop\K8s網站
.\deploy.ps1
```

腳本會自動完成：
1. 檢查 Docker 狀態
2. 啟動 Minikube（含 Containerd runtime）
3. 依 Dockerfile build 自訂 nginx 映像
4. Apply deployment.yaml / service.yaml
5. 觸發 rollout 並等待 Pod ready
6. 開啟瀏覽器

---

## 手動執行（若不用 deploy.ps1）

```powershell
cd C:\Users\jason\Desktop\K8s網站

# 啟動 Minikube
.\bin\minikube.exe start --driver=docker --container-runtime=containerd

# Build 映像進 Minikube
.\bin\minikube.exe image build -t syswatch-nginx:1.0.0 .

# 套用 K8s 資源
.\bin\kubectl.exe apply -f deployment.yaml
.\bin\kubectl.exe apply -f service.yaml

# 取得網址
.\bin\minikube.exe service nginx-service --url
```

---

## 修改 HTML 後重新部署

```powershell
# 1. 重新 build 映像
.\bin\minikube.exe image build -t syswatch-nginx:1.0.0 .

# 2. 觸發 Deployment 滾動更新（同 tag 必須手動觸發）
.\bin\kubectl.exe rollout restart deployment/nginx-deployment

# 3. 等新版 Pod 就緒
.\bin\kubectl.exe rollout status deployment/nginx-deployment
```

---

## 注意事項

- **不需要每次重新 apply YAML**，K8s 資源已存在叢集中，Minikube 重啟後會自動恢復
- **修改 deployment.yaml / service.yaml** 才需要重新執行 `kubectl apply`
- **修改 html/index.html** 必須重 build 映像 + rollout restart
- **重 build 同 tag 映像不會自動觸發 rollout**，K8s 看 image tag 沒變就不重啟 Pod，必須手動 `kubectl rollout restart`

---

## 確認部署狀態（選用）

```powershell
# 確認 Pod 是否正常運行（STATUS 應為 Running）
.\bin\kubectl.exe get pods

# 確認 Service 是否存在
.\bin\kubectl.exe get services

# 查看 Pod 日誌（除錯用）
.\bin\kubectl.exe logs -l app=nginx --tail=50

# 確認 Minikube 內已有自訂映像
.\bin\minikube.exe image ls | Select-String syswatch-nginx

# 查看 HPA 狀態（要看 CPU 指標必須 metrics-server 已啟用）
.\bin\kubectl.exe get hpa
.\bin\kubectl.exe top pods -l app=nginx

# 手動觸發 HPA 擴展測試（製造 CPU 壓力）
.\bin\kubectl.exe run -it --rm load-gen --image=busybox --restart=Never -- /bin/sh -c "while true; do wget -q -O- http://nginx-service.default; done"
# 開另一個視窗持續觀察 HPA：
# .\bin\kubectl.exe get hpa nginx-hpa --watch
```
