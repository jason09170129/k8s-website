# 面試備忘卡（單頁版）

> 印出來放手邊；面試前 5 分鐘掃過一遍即可。

---

## 開場 30 秒（背起來）

> 「這是我做的 K8s 部署示範。為了貼近正式環境，我刻意整合了完整的 DevOps 工作流：**Git 版控、Docker 自己 build 映像、Containerd 當 K8s runtime、GitLab CI/CD 四階段 pipeline**。我先跑一次，再帶您看幾個關鍵設計決策。」

---

## 技術棧一句話

| 技術 | 一句話講為什麼 |
|------|---------------|
| **Docker** | 用 Dockerfile 把 HTML 打包進 nginx 映像，配合 image tag 做版本控管 |
| **Containerd** | K8s 1.24+ 的預設 runtime，直接 CRI 呼叫，去掉 dockershim 中間層 |
| **Kubernetes** | 自我修復、聲明式管理；用 Deployment + Service + 探針示範核心能力 |
| **Git** | 部署設定即程式碼（IaC），所有變更可追蹤可回退 |
| **GitLab CI** | build → test → push → deploy 四階段，正式部署設 manual 留審核點 |

---

## Demo 流程（3 分鐘）

```
1. ls                        ← 看專案結構
2. git log --oneline         ← 看 commit 紀錄
3. .\deploy.ps1              ← 一鍵跑（邊跑邊講以下要點）
   ├─ 「containerd 2.2.1」    ← 指給面試官看
   ├─ docker build 進度       ← 「正在打包 HTML」
   ├─ Pod 1/1 Running        ← 「探針通過」
   └─ 瀏覽器跳出網頁          ← 結束
```

**萬一現場壞掉**：開 `preview.html` 看 HTML、或 `kubectl get pods` 證明上次部署還在。

---

## 走讀檔案順序（5 分鐘）

| 順序 | 檔案 | 重點一句話 |
|------|------|-----------|
| 1 | `Dockerfile` | alpine 縮到 50MB、HEALTHCHECK 讓 Docker 也能探活 |
| 2 | `deployment.yaml` | **resources / liveness / readiness** 三個是自我修復核心 |
| 3 | `.gitlab-ci.yml` | 4 階段切分；test 含 Trivy 掃 CVE；deploy 設 manual |
| 4 | `.gitignore` | bin/ + *.exe 不進版控 |
| 5 | `K8s技術說明.md` | 「您可以挑題目考我」（主動讓對方出題） |

---

## 數字記憶卡

- Base image：`nginx:1.27-alpine` ≈ **50 MB**
- NodePort：**30080**（K8s NodePort 範圍 30000–32767）
- Pod resources：requests `50m / 32Mi`，limits `200m / 128Mi`
- Probe：liveness 10s 一次、readiness 5s 一次
- Replicas：**3**（消除 SPOF）
- HPA：min `2`、max `10`、CPU `50%` 觸發
- K8s 移除 dockershim 版本：**1.24**
- CI runner 免費額度：400 分鐘/月

---

## 「如果被問 X，就答 Y」地雷題

| 問題 | 答案核心 |
|------|---------|
| 為何不用 ConfigMap 掛 HTML？ | 想配合 image tag 做版本控管，不可變基礎設施 |
| 為何不用 `:latest`？ | Pod 重啟可能拉不同版本，違反不可變原則 |
| Docker 不是已經淘汰？ | 是 dockershim 被淘汰，不是 Docker；Docker 仍是最好的本機開發工具 |
| GitLab 帳號呢？ | pipeline 設計重點是架構切分，今天 demo 只在本機跑（誠實但有準備的答法） |
| 為何 deploy 設 manual？ | production 部署需要人為審核作最後防線 |
| 有沒有監控/log？ | 「目前沒做，下一步會加 Prometheus + Loki」（誠實 + 展現規劃） |

---

## 加分句（找機會塞）

- 「我刻意 `--container-runtime=containerd` 啟動，貼近正式叢集」
- 「image tag 用 `$CI_COMMIT_SHORT_SHA`，每個 commit 對應唯一映像，便於精準回退」
- 「Trivy 掃 CVE 我設成 `allow_failure: true`，不擋 pipeline 但會留警示」
- 「`kubectl rollout undo` 可以一鍵回退到上個版本」
- 「replicas 設 3 + HPA min 2，**就算一個 Pod 掛、另一個 scale 中，也至少還有一個在服務**」
- 「HPA `behavior` 我把擴展設積極、收縮設保守，避免流量抖動時 Pod 數量震盪」

---

## 結尾收話

> 「目前這個版本是 v1.0.0。下一步我會加上 **Prometheus + Grafana 做監控**、**ArgoCD 做 GitOps 自動部署**、把 ConfigMap 改用 **Helm Chart 管理**。請問您這邊有沒有想深入問的部分？」
