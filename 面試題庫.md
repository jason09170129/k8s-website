# 面試題庫（含模範答案）

> 每題答案壓在 30–60 秒可講完。背重點，不背逐字稿。

---

## A. Docker / 容器化（5 題）

### A1. Docker 和 Containerd 有什麼差別？K8s 為什麼從 Docker 改用 Containerd？

**核心答**：
- Docker 是**完整工具集**（CLI + daemon + build + image management），給開發者用
- Containerd 是 Docker 抽出的**底層 runtime**，只負責執行容器，給系統用
- K8s 1.24 之前透過 dockershim 呼叫 Docker，但 dockershim 是 K8s 額外維護的轉接層，**維護成本高、多一層 overhead**
- 1.24 起 dockershim 移除，K8s 直接透過 CRI（Container Runtime Interface）呼叫 Containerd，**更輕量、更貼近 OCI 標準**

**加分**：「所以本機開發我用 Docker，但叢集 runtime 設成 Containerd，這也是為什麼我 `minikube start --container-runtime=containerd`。」

---

### A2. 為什麼用 alpine 版本的 nginx？

**核心答**：
- 體積小：標準版 `nginx:1.27` 約 200MB，alpine 版只有 50MB
- 縮小**攻擊面**（套件少、CVE 少）
- 拉取快、部署快，配合 K8s 多 Pod 場景特別明顯

**注意**：alpine 用 musl libc 而非 glibc，少數需要動態連結的程式可能不相容；但對 nginx 這種靜態 binary 不影響。

---

### A3. 為什麼要寫 HEALTHCHECK？K8s 已經有 livenessProbe 了不就夠？

**核心答**：
- HEALTHCHECK 是 **Docker 層級**的探活，K8s 探針是**叢集層級**
- 兩者不衝突，HEALTHCHECK 讓映像在「裸 Docker run」場景（例如本機驗證、CI build 後測試）也能自動探活
- K8s 部署時實際生效的是 livenessProbe / readinessProbe，HEALTHCHECK 不會被 K8s 用到

**加分**：「映像有 HEALTHCHECK 也方便 `docker ps` 直接看狀態，不用另外寫腳本。」

---

### A4. 為什麼 image tag 不該用 `:latest`？

**核心答**：
- `:latest` 是**浮動標籤**，Pod 重啟可能拉到不同版本，違反**不可變基礎設施（Immutable Infrastructure）**原則
- 出問題時不知道哪個版本，無法精準回退
- 我用 `$CI_COMMIT_SHORT_SHA` 當 tag，每個 commit 對應唯一映像，**git commit 跟 image tag 一對一**

**加分**：「同 tag 重新 build，K8s 看 tag 沒變就不會自動 rollout，必須手動 `kubectl rollout restart`——這也是一個雷。」

---

### A5. Dockerfile 多階段建構（multi-stage build）什麼時候用？

**核心答**：
- 編譯型語言（Go、Java、Rust）必用：第一階段拿大映像（含編譯器）build、第二階段用小映像（如 alpine 或 distroless）只塞執行檔
- 本專案是純靜態 HTML 不需要編譯，所以單階段就夠
- 多階段可以把 1GB 的映像壓到 20MB

**加分（如果背景是 Go）**：「我的 SysWatch 專案就用了多階段，從 golang:1.22-alpine build、再 COPY 到 alpine:3.20，最終映像 < 30MB。」

---

## B. Kubernetes（6 題）

### B1. 講一下 K8s 三個最基本的物件：Pod / Deployment / Service

**核心答**：
- **Pod** = K8s 最小部署單位，內含一或多個容器，**共用網路與儲存**
- **Deployment** = 管理 Pod 的部署策略，負責**維持指定 replica 數**、**滾動更新**、**回退**
- **Service** = 把一組 Pod 包成穩定的網路入口，**提供固定 IP + DNS**，Pod 重啟換 IP 也不影響

**加分**：「Service 用 selector 找 Pod，是用 label 配對的，這是 K8s 解耦設計的核心。」

---

### B2. livenessProbe 跟 readinessProbe 差在哪？

**核心答**：
- **livenessProbe**：探活，失敗 → K8s **重啟 Pod**（救活）
- **readinessProbe**：探就緒，失敗 → K8s **把 Pod 從 Service 流量移除**，但 Pod 還活著（不被打擾，給它時間恢復）
- 用途不同：livenessProbe 對付死掉的 Pod，readinessProbe 對付「還沒準備好接流量」的 Pod（例如啟動中、暖機中、暫時過載）

**加分**：「常見錯誤是 livenessProbe 設太敏感，導致暫時慢回應就被殺重啟，反而把 Pod 殺進 CrashLoopBackOff。」

---

### B3. Pod 為什麼會 CrashLoopBackOff？怎麼除錯？

**核心答**：
- 意思是 Pod 反覆啟動失敗，K8s 用**指數退避**降低重試頻率（10s → 20s → 40s ... 最多 5 分鐘）
- 排查順序：
  1. `kubectl describe pod <name>` → 看 Events 段，常見原因：映像拉不到、OOMKilled、探針失敗
  2. `kubectl logs <name> --previous` → 看上次掛掉的 log（current log 沒用因為還沒起來）
  3. `kubectl get events --sort-by=.lastTimestamp` → 看叢集級事件

**加分**：「我常用 `kubectl debug` 開一個 ephemeral container 進去看檔案系統，比 exec 更不破壞現場。」

---

### B4. NodePort 跟 LoadBalancer 跟 Ingress 差在哪？

**核心答**：
- **NodePort**：每個 Node 開一個 port（30000–32767），外部透過 `<Node IP>:<NodePort>` 存取——**本機開發 / 簡單測試用**
- **LoadBalancer**：跟雲端整合，自動建一個雲端 LB（AWS ELB、GCP LB），給一個 public IP——**正式雲端環境用**
- **Ingress**：L7 反向代理（基於 hostname / path 路由），多個 service 共用一個 LB——**正式環境大量 service 用**

**加分**：「我這個 demo 用 NodePort 是因為 Minikube 本機環境；正式環境我會改 Ingress + nginx-ingress-controller。」

---

### B5. 怎麼水平擴展？HPA 是什麼？

**核心答**：
- 手動：`kubectl scale deployment nginx-deployment --replicas=5`
- 自動：**HPA（HorizontalPodAutoscaler）**，根據 CPU/記憶體/自訂指標自動調整 replica 數
- HPA 需要 **metrics-server** 才能拿到 Pod 資源用量

**範例**：「設 minReplicas=2、maxReplicas=10、CPU target 50%——當 CPU 平均 > 50% 就加 Pod，< 50% 就減。」

**加分**：「HPA 配合 `resources.requests` 才會準確；沒設 requests 的 Pod，HPA 算出來的 % 是錯的。」

---

### B6. Pod 之間怎麼通訊？

**核心答**：
- 同 Pod 內容器：透過 `localhost` 通訊（共用網路命名空間）
- 跨 Pod：透過 **Service DNS**（`<service-name>.<namespace>.svc.cluster.local`，或同 namespace 直接用 `<service-name>`）
- 不要直接用 Pod IP，因為 Pod 隨時可能被重建換 IP

**加分**：「K8s 內部 DNS 由 CoreDNS 提供，是叢集核心元件，這也是為什麼 `kubectl get pods -n kube-system` 一定會看到 coredns pod。」

---

## C. Git / GitLab CI（5 題）

### C1. 為什麼 deploy stage 設 `when: manual`？

**核心答**：
- **production 部署風險高**，全自動會放大失誤影響範圍
- 實務模式：「自動部 staging、手動部 production」，留人為審核作最後防線
- 配合 `environment: production`，GitLab 會在 UI 留下部署歷史，可一鍵看誰部的、何時部的

**加分**：「如果要全自動，可以改用 GitOps 模式（ArgoCD/Flux 監聽 git）——但通常會搭 PR 流程或 approval 機制，不是裸自動。」

---

### C2. GitLab CI 的 stages、jobs、artifacts 關係？

**核心答**：
- **stage** = 階段（build / test / push / deploy），同 stage 內 jobs **並行**，stage 間**串行**
- **job** = 實際執行單位，跑在 runner 上
- **artifacts** = job 產出的檔案（如本專案 build stage 產 `image.tar`），可傳給後續 job 用
- 我的 test stage 有 lint-yaml 跟 scan-image 兩個 job，**會並行跑**

**加分**：「`needs` 關鍵字可以打破 stage 順序，建一個 DAG 而不是線性 pipeline，pipeline 整體更快。」

---

### C3. 為什麼用 commit SHA 當 image tag？

**核心答**：
- 每個 commit **唯一對應**一個映像，不可變
- 出問題時：看 GitLab pipeline → 找到對應 commit SHA → `kubectl set image deployment/xxx nginx=registry/image:abc123` 一鍵回退
- 避免 `:latest` 那種「不知道現在 prod 跑哪版」的窘境

**加分**：「另一個常見做法是 git tag 觸發 release pipeline、用 git tag 當 image tag（如 v1.2.3），語意更清楚。」

---

### C4. 為什麼 .gitignore 排除 bin/？

**核心答**：
- bin/ 內是 minikube.exe 跟 kubectl.exe，**單檔 50MB+**，二進位檔不該進 git
- 不同人作業系統不同（Windows / Mac / Linux），二進位檔不通用
- 正確做法：寫在 README，請使用者自己下載，或用 package manager（brew / scoop / winget）裝

**加分**：「我這次保留 bin/ 在資料夾內是給面試官方便執行；長期維護的專案不會這樣做。」

---

### C5. Trivy 是什麼？為什麼設 allow_failure: true？

**核心答**：
- **Trivy** 是 Aqua Security 開源的容器漏洞掃描工具，掃 image 內套件的 CVE
- 設 `allow_failure: true` 是因為：
  - base image（nginx, alpine）可能有未修補 CVE，**不是我能控制**
  - 如果擋 pipeline，會卡在無解狀態
  - 改成「不擋但留警示」，**讓資安事件可見**比強制 fail 更實用
- 真實正式環境會搭 SBOM + 漏洞管理流程，這只是入門做法

---

## D. 設計決策 / 系統思考（5 題）

### D1. 為什麼從 ConfigMap 改成把 HTML 打包進映像？

**核心答**：
- ConfigMap 適合「**設定**」（< 1MB 文字檔），應用程式內容該隨映像一起 version
- 配合 image tag 做精準版本控管：**v1.0.0 映像永遠對應 v1.0.0 的 HTML**
- 修 HTML 忘了 apply ConfigMap 會導致「映像版本一致但內容不一致」，破壞可重現性
- 改成 Dockerfile + COPY，CI/CD 流程更乾淨

**加分**：「但 ConfigMap 還是有用場景——例如環境差異設定（DB 連線字串、feature flag），那種隨環境變不隨版本變的東西。」

---

### D2. 假設網站流量突然 100 倍，你會怎麼處理？

**核心答（分層回答）**：
1. **短期**：`kubectl scale --replicas=20` 緊急加 Pod
2. **中期**：設 HPA 自動擴展（CPU/QPS 觸發）
3. **長期**：
   - 加 CDN（CloudFlare / CloudFront）抓靜態資源
   - Service 改 LoadBalancer + Ingress
   - 加 cluster autoscaler 讓 Node 也能自動加
   - 上 Redis 快取熱門資料
4. **觀察**：先 `kubectl top pods` 看是 CPU 還是記憶體瓶頸，再決定要不要垂直擴展（給單 Pod 更多資源）

**加分**：「靜態網頁這種特別適合 CDN，K8s 那層幾乎不會被打到。」

---

### D3. 假設 Pod 跑得好好的突然開始 OOMKilled，怎麼查？

**核心答**：
1. `kubectl describe pod <name>` → 看 Last State，會寫 `OOMKilled`
2. 看 `resources.limits.memory` 設多少
3. 看實際用量：`kubectl top pod <name>` 或 Prometheus / Grafana 歷史曲線
4. 判斷是：
   - **真記憶體不夠** → 調高 limit 或優化程式（profile + leak hunting）
   - **記憶體洩漏** → 進去 pprof / heap dump 找
   - **流量暴增** → 配 HPA

**加分**：「OOMKilled 比較難的場景是『偶爾才發生』，這時要靠 Prometheus 抓記憶體 spike 跟業務指標關聯，不能只看單一時間點。」

---

### D4. K8s 的 self-healing 怎麼做到的？

**核心答**：
- 核心是 **Controller Pattern**：每個 Controller 有「desired state」跟「current state」，不斷 reconcile
- 例子：
  - **Deployment Controller** 看 replicas: 3，發現只有 2 個 Pod → 建一個新的
  - **ReplicaSet Controller** 看到 Pod 掛了 → 立即補建
  - **Node Controller** 看到 Node 沒回應 → 把 Node 上的 Pod 標記重新調度
- 這就是宣告式（declarative）的精髓：**你只說想要什麼，不說怎麼做**

**加分**：「Operator pattern 就是把這套 controller 思維延伸到自訂資源，例如 ArgoCD Operator、PostgreSQL Operator。」

---

### D5. 如果讓你重做這個專案，會改哪些？

**核心答**（誠實 + 展現自省）：
- 加 **Prometheus + Grafana** 監控（目前完全沒指標）
- 用 **Helm Chart** 包裝 K8s 資源，方便多環境部署
- 加 **Ingress + cert-manager**，HTTPS 才是正式環境基本
- pipeline 加**自動 staging 部署 + smoke test**，目前只到 build 階段是測試
- 用 **ArgoCD** 改 GitOps，pipeline 不直接 kubectl，而是 commit 到 gitops repo，ArgoCD 自動 sync

**加分**：「但這些都要平衡複雜度，新專案我不會一開始就上全套；先讓核心跑起來再逐步加。」

---

## E. 跳脫題目 / 行為面試（3 題）

### E1. 為什麼選 SRE / DevOps 方向？

（這題你自己準備，我不替你答；但回答時要 **連結到具體經驗**，不要只講「我對自動化有熱情」）

**結構建議**：
- 一個具體事件（之前做 X 時遇到 Y 問題）
- 那次解決問題的過程中發現我喜歡 Z（自動化、系統穩定性、跨團隊協作）
- 所以開始學 W、做這個專案來累積

---

### E2. 這個專案花了多久？

**核心答（誠實版）**：
- 「原本是國泰面試的小作業，大概一個下午做完基本部署」
- 「但為了把它變成完整 portfolio，花了大約 X 小時做擴充——加 Dockerfile、寫 CI 設計、整理面試題庫」
- 「我覺得**講清楚為什麼這樣設計**比寫更多功能重要，所以時間多花在思考跟文件」

---

### E3. 你遇過最大的技術困難是什麼？

（這題也需要你自己準備，但**結構**可以套）

**STAR 結構**：
- **S**ituation：什麼背景
- **T**ask：你要解決什麼
- **A**ction：你做了什麼（**重點**，要具體：用了什麼工具、嘗試了什麼方法、踩了哪些坑）
- **R**esult：結果怎樣（量化最好：減少 X% 故障、提升 Y 倍效能）

**反例**（不要這樣答）：「我修過一個很難的 bug 最後解掉了」——零資訊。

---

## 答題原則

1. **先講結論，再展開**：30 秒能講完的答案，前 5 秒就要給出 punchline
2. **舉具體例子勝過抽象描述**：「我用過 X 解決 Y」比「我對 X 很熟」強 10 倍
3. **不會就誠實說**：「這部分我沒實際做過，但我理解原理是 ...」，然後引導回你會的
4. **每題收尾留鉤子**：「如果有興趣可以深入聊 Z」，主動引導對方往你強項問
